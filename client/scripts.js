import {
  activateDropdown,
  handleStarBtn,
  renderRepos,
  renderTotalCount,
  renderUser,
} from "./lib/index.js";

activateDropdown();
renderUser();
renderTotalCount();

// TODO: 'then'...add 👩🏾‍🎤 button handlers
renderRepos();
